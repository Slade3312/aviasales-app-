const initialState = {
  filter: {
    all: false,
    noneTransfer: false,
    oneTransfer: false,
    twoTransfer: false,
    threeTransfer: false,
  },
};

export default function reducer(state = initialState) {
  return state;
}
